type: app
description: Default stackfile usecase description
# template used to create the project
template: java-starter/<template-name>
inputs:
  some_input: value
# list of plugins to be applied after project creation
plugins:
  - plugin: java-starter/<plugin-name>
    inputs:
      some_input: value
      some_list:
        - item1
        - item2
